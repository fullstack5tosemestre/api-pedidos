name: Build and publish to docker hub
on:
  push:
    branches:
      - main
    paths:
      - "Dockerfile"
      - "src/**"
  workflow_dispatch:

jobs:
  publish-image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: build .jar file
        run: |
          chmod +x ./mvnw
          ./mvnw clean package

      - name: build image
        run: |
          docker build -t pedidos:latest .
          docker tag pedidos:latest ${{ secrets.ECR_REPO_URL_API_PEDIDOS }}:latest

      - name: Push API Pedidos Image to ECR
        run: |
          docker push ${{ secrets.ECR_REPO_URL_API_PEDIDOS }}:latest

      - name: Create SSH Key
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > key.pem
          chmod 400 key.pem

      - name: Deploy API Pedidos to Private EC2
        run: |
          ssh -o StrictHostKeyChecking=no \
          -i key.pem \
          ec2-user@${{ secrets.EC2_API_GATEWAY_PUBLIC_IP }} \
          "ssh -o StrictHostKeyChecking=no \
          -i ~/DevOps.pem \
          ec2-user@${{ secrets.EC2_API_PEDIDOS_PRIVATE_IP }} \
          'aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 089485767547.dkr.ecr.us-east-1.amazonaws.com && \
          docker pull ${{ secrets.ECR_REPO_URL_API_PEDIDOS }}:latest && \
          docker rm -f pedidos || true && \
          docker run -d \
          -p 8081:8081 \
          -e SPRING_DATASOURCE_URL=jdbc:mysql://${{ secrets.EC2_DB_PRIVATE_IP }}:3306/db_pedidos \
          -e SPRING_DATASOURCE_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }} \
          -e INVENTORY_API_BASE_URL=http://${{ secrets.EC2_API_GATEWAY_PUBLIC_IP }}/api/v1/products \
          --name pedidos \
          --restart unless-stopped \
          ${{ secrets.ECR_REPO_URL_API_PEDIDOS }}:latest \
          && docker image prune -af'"
